{
  "name": "livada-mea-dashboard",
  "version": "2.0.0",
  "description": "Dashboard PWA pentru livada semi-comerciala — 20 specii, zona Nadlac/Arad",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "node scripts/update-deploy-date.js && node scripts/minify-html.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@upstash/redis": "^1.37.0",
    "@vercel/blob": "^2.3.3"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.4",
    "vitest": "^4.1.4"
  }
}
